#include  <stdio.h>
/**
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x,y;

	x = '1';
	while (x < '100')
	{
		if (x < '10')
		{
			x = '0' + x;
			putchar(x);
			putchar(',');
			putchar(' ');
		}
		else if (x >= '10')
		{
			if (x !combo already exists)
			{
				putchar(x);
			}
			else
			{
				break;
			}
		}
		putchar('\n');
		return (0);
}
