#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;

	x = 'a';
	while (x <= 'z')
	{
		putchar(x | grep [eq]);
		x++;
	}
	putchar('\n');
	return (0);
}
