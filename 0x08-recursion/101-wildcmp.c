#include "main.h"

/**
 * wildcmp - compares two string
 * @s1: String 1
 * @s2: String 2
 *
 * Return: 1 if @s1 and @s2 are equal
 * 0 if @s1 and @s2 are not equal
 */
int wildcmp(char *s1, char *s2)
{
	if (s1 == s2)
	{
		return (1);
	}
}
