#include <stdio.h>

/**
 * _strspn - Entry point
 * @s: Parameter
 * @accept: Parameter
 *
 * Return: strspn
 */

unsigned int _strspn(char *s, char *accept)
{
	unsigned int i = 0;

	printf("%s\n", s);
	printf("%s\n", accept);

	return (i);
}
