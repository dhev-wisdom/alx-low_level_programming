#include <stdio.h>

/**
 * _strcmp - Entry point
 * @s1: Parameter
 * @s2: Parameter
 *
 * Return: 0 on Success, 1 on Failure
 */

int _strcmp(char *s1, char *s2)
{
	printf("%s\n", s1);
	printf("%s\n", s2);

	return (0);
}
