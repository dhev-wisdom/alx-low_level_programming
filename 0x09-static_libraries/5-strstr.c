#include <stdio.h>
#include "main.h"

/**
 * _strstr - Entry point
 * @haystack: Parameter
 * @needle: Parameter
 *
 * Return: strstr
 */

char *_strstr(char *haystack, char *needle)
{
	char *s = "strcat";

	printf("%s\n", haystack);
	printf("%s\n", needle);

	return (s);
}
