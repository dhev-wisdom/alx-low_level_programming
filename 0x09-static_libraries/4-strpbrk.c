#include <stdio.h>
#include "main.h"

/**
 * _strpbrk - Entry point
 * @s: Parameter
 * @accept: Parameter
 *
 * Return: strpbrk
 */

char *_strpbrk(char *s, char *accept)
{
	char *st = "strcat";

	printf("%s\n", s);
	printf("%s\n", accept);

	return (st);
}
